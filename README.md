# Run project with docker-compose

## Application launch

- first time
```sh
docker compose up -d --build
```

- subsequent times
```sh
docker compose up -d --build
```

- disabling a container
```sh
docker compose down
```

- viewing logs
```sh
docker compose logs -f
```
